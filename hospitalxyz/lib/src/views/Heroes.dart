import 'package:flutter/material.dart';

class Herois extends StatelessWidget {
  const Herois({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Herois")),
    );
  }
}
